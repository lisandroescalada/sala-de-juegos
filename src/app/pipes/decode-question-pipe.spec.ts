import { DecodeQuestionPipe } from './decode-question-pipe';

describe('DecodeQuestionPipe', () => {
  it('create an instance', () => {
    const pipe = new DecodeQuestionPipe();
    expect(pipe).toBeTruthy();
  });
});
