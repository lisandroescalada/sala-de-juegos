import { HighlightMessages } from './highlight-messages';

describe('HighlightMessages', () => {
  it('should create an instance', () => {
    const directive = new HighlightMessages();
    expect(directive).toBeTruthy();
  });
});
